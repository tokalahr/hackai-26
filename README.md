
  # Multi-page web app prototype

  This is a code bundle for Multi-page web app prototype. The original project is available at https://www.figma.com/design/JeWwRuZSn0xdQkbZaVKn1w/Multi-page-web-app-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  